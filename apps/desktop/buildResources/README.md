# Build Resources

This folder is reserved for Electron packaging assets such as app icons,
entitlements, and notarization configuration.

The initial macOS package is intentionally unsigned for local/internal validation.
